name: Build

on: 
  pull_request:
  push:
    branches:
      - master

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: yarn
    - run: yarn build
    - uses: actions/upload-artifact@v2
      with:
        name: stats.html
        path: stats.html
